-- BLOX FRUITS PAID VERSION - WORKING (NO LICENSE CHECK, DIRECT LOAD)
-- Password: astro

-- LOGIN GUI
local loginGui = Instance.new("ScreenGui")
loginGui.Name = "LoginGUI"
loginGui.Parent = game:GetService("CoreGui")

local loginFrame = Instance.new("Frame")
loginFrame.Size = UDim2.new(0, 250, 0, 150)
loginFrame.Position = UDim2.new(0.5, -125, 0.5, -75)
loginFrame.BackgroundColor3 = Color3.new(0, 0, 0)
loginFrame.BackgroundTransparency = 0.1
loginFrame.BorderSizePixel = 3
loginFrame.BorderColor3 = Color3.new(1, 0.5, 0)
loginFrame.Parent = loginGui

local titleLabel = Instance.new("TextLabel")
titleLabel.Size = UDim2.new(1, 0, 0, 40)
titleLabel.Text = "BLOX FRUITS PAID"
titleLabel.TextColor3 = Color3.new(1, 1, 0)
titleLabel.BackgroundTransparency = 1
titleLabel.Parent = loginFrame

local passwordBox = Instance.new("TextBox")
passwordBox.Size = UDim2.new(0, 200, 0, 40)
passwordBox.Position = UDim2.new(0.5, -100, 0, 50)
passwordBox.PlaceholderText = "Password"
passwordBox.Text = ""
passwordBox.BackgroundColor3 = Color3.new(0.2, 0.2, 0.2)
passwordBox.TextColor3 = Color3.new(1, 1, 1)
passwordBox.Parent = loginFrame

local submitBtn = Instance.new("TextButton")
submitBtn.Size = UDim2.new(0, 100, 0, 35)
submitBtn.Position = UDim2.new(0.5, -50, 0, 100)
submitBtn.Text = "SUBMIT"
submitBtn.BackgroundColor3 = Color3.new(0.2, 0.6, 0.2)
submitBtn.Parent = loginFrame

local errorText = Instance.new("TextLabel")
errorText.Size = UDim2.new(1, 0, 0, 25)
errorText.Position = UDim2.new(0, 0, 0, 140)
errorText.Text = ""
errorText.TextColor3 = Color3.new(1, 0, 0)
errorText.BackgroundTransparency = 1
errorText.Parent = loginFrame

local function loadMainScript()
    loginGui:Destroy()
    
    -- MAIN SCRIPT START
    local Players = game:GetService("Players")
    local RunService = game:GetService("RunService")
    local UserInputService = game:GetService("UserInputService")
    local LocalPlayer = Players.LocalPlayer
    local Camera = workspace.CurrentCamera
    local VirtualUser = game:GetService("VirtualUser")
    local ReplicatedStorage = game:GetService("ReplicatedStorage")

    -- SETTINGS (ALL OFF BY DEFAULT)
    local espEnabled = true
    local autoFarmEnabled = false
    local oneHitKillEnabled = false
    local autoCollectEnabled = false
    local teleportToBossEnabled = false
    local autoQuestEnabled = false
    local fastAttackEnabled = false
    local noClipEnabled = false

    -- FPS BOOSTER
    local function boostFPS()
        pcall(function()
            game:GetService("Lighting").ShadowSoftness = 0
            game:GetService("Lighting").Technology = Enum.Technology.Compatibility
            settings().Rendering.QualityLevel = 1
            for _, v in pairs(workspace:GetDescendants()) do
                if v:IsA("Decal") or v:IsA("Texture") or v:IsA("ParticleEmitter") then
                    v.Enabled = false
                end
            end
        end)
    end
    boostFPS()

    -- GUI
    local screenGui = Instance.new("ScreenGui")
    screenGui.Name = "BloxFruitsGUI"
    screenGui.Parent = game:GetService("CoreGui")
    screenGui.ResetOnSpawn = false

    local mainFrame = Instance.new("Frame")
    mainFrame.Size = UDim2.new(0, 200, 0, 350)
    mainFrame.Position = UDim2.new(0.1, 0, 0.3, 0)
    mainFrame.BackgroundColor3 = Color3.new(0, 0, 0)
    mainFrame.BackgroundTransparency = 0.3
    mainFrame.BorderSizePixel = 2
    mainFrame.BorderColor3 = Color3.new(1, 0.5, 0)
    mainFrame.Parent = screenGui

    local title = Instance.new("TextLabel")
    title.Size = UDim2.new(1, 0, 0, 30)
    title.Text = "BLOX FRUITS PAID | astro"
    title.TextColor3 = Color3.new(1, 1, 0)
    title.BackgroundTransparency = 1
    title.Parent = mainFrame

    local hideBtn = Instance.new("TextButton")
    hideBtn.Size = UDim2.new(0, 50, 0, 25)
    hideBtn.Position = UDim2.new(0.7, 0, 0, 2)
    hideBtn.Text = "━"
    hideBtn.BackgroundColor3 = Color3.new(0.3, 0.3, 0.3)
    hideBtn.Parent = mainFrame

    local floatingCircle = Instance.new("TextButton")
    floatingCircle.Size = UDim2.new(0, 55, 0, 55)
    floatingCircle.Position = UDim2.new(0.02, 0, 0.85, 0)
    floatingCircle.BackgroundColor3 = Color3.new(1, 0.5, 0)
    floatingCircle.BackgroundTransparency = 0.2
    floatingCircle.Text = "B"
    floatingCircle.TextColor3 = Color3.new(1, 1, 1)
    floatingCircle.TextSize = 28
    floatingCircle.TextScaled = true
    floatingCircle.Parent = screenGui
    floatingCircle.Visible = false

    local function createButton(name, y, text)
        local btn = Instance.new("TextButton")
        btn.Name = name
        btn.Size = UDim2.new(0, 170, 0, 35)
        btn.Position = UDim2.new(0, 15, 0, y)
        btn.Text = text
        btn.BackgroundColor3 = Color3.new(0.6, 0.2, 0.2)
        btn.Parent = mainFrame
        return btn
    end

    local espBtn = createButton("EspBtn", 35, "ESP: ON")
    local farmBtn = createButton("FarmBtn", 75, "AUTO FARM: OFF")
    local oneHitBtn = createButton("OneHitBtn", 115, "1 HIT KILL: OFF")
    local collectBtn = createButton("CollectBtn", 155, "AUTO COLLECT: OFF")
    local bossBtn = createButton("BossBtn", 195, "TELEPORT BOSS: OFF")
    local questBtn = createButton("QuestBtn", 235, "AUTO QUEST: OFF")
    local fastBtn = createButton("FastBtn", 275, "FAST ATTACK: OFF")
    local noclipBtn = createButton("NoclipBtn", 315, "NO CLIP: OFF")

    espBtn.BackgroundColor3 = Color3.new(0.2, 0.8, 0.2)

    -- GET ALL ENEMIES
    local function getAllEnemies()
        local enemies = {}
        for _, child in pairs(workspace:GetDescendants()) do
            if child:FindFirstChild("Humanoid") and child.Humanoid.Health > 0 and child:FindFirstChild("HumanoidRootPart") then
                if child ~= LocalPlayer.Character and not child:IsDescendantOf(LocalPlayer.Character) then
                    table.insert(enemies, child)
                end
            end
        end
        return enemies
    end

    -- 1 HIT KILL
    local function oneHitKill()
        if not oneHitKillEnabled then return end
        local enemies = getAllEnemies()
        for _, enemy in pairs(enemies) do
            if enemy and enemy:FindFirstChild("Humanoid") and enemy.Humanoid.Health > 0 then
                enemy.Humanoid.Health = 0
            end
        end
    end

    -- AUTO FARM
    local function autoFarm()
        if not autoFarmEnabled then return end
        local enemies = getAllEnemies()
        if #enemies == 0 then return end
        
        local nearest = nil
        local shortestDist = math.huge
        for _, enemy in pairs(enemies) do
            local root = enemy:FindFirstChild("HumanoidRootPart")
            if root and LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart") then
                local dist = (root.Position - LocalPlayer.Character.HumanoidRootPart.Position).Magnitude
                if dist < shortestDist then
                    shortestDist = dist
                    nearest = enemy
                end
            end
        end
        
        if nearest and nearest.HumanoidRootPart then
            local hrp = LocalPlayer.Character.HumanoidRootPart
            if hrp then
                hrp.CFrame = nearest.HumanoidRootPart.CFrame * CFrame.new(0, 0, 5)
            end
            wait(0.05)
            pcall(function()
                VirtualUser:Button1Down(Vector2.new(0,0))
                wait(0.02)
                VirtualUser:Button1Up(Vector2.new(0,0))
            end)
        end
    end

    -- AUTO COLLECT
    local function autoCollect()
        if not autoCollectEnabled then return end
        for _, v in pairs(workspace:GetDescendants()) do
            if v:IsA("Model") and v:FindFirstChild("ClickDetector") then
                local name = v.Name:lower()
                if name:find("fruit") or name:find("chest") or name:find("drop") or name:find("beli") then
                    local cd = v:FindFirstChild("ClickDetector")
                    if cd and LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart") then
                        local dist = (v:GetPivot().Position - LocalPlayer.Character.HumanoidRootPart.Position).Magnitude
                        if dist < 80 then
                            fireclickdetector(cd)
                            wait(0.05)
                        end
                    end
                end
            end
        end
    end

    -- TELEPORT TO BOSS
    local function teleportToBoss()
        if not teleportToBossEnabled then return end
        local bossNames = {"Dragon", "Don Swan", "Magma", "Awakened Ice Admiral", "Darkbeard", "Cake Queen", "Dough King"}
        for _, name in pairs(bossNames) do
            local boss = workspace:FindFirstChild(name)
            if boss and boss:FindFirstChild("HumanoidRootPart") and LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart") then
                LocalPlayer.Character.HumanoidRootPart.CFrame = boss.HumanoidRootPart.CFrame * CFrame.new(0, 5, 8)
                break
            end
        end
    end

    -- AUTO QUEST
    local function autoQuest()
        if not autoQuestEnabled then return end
        pcall(function()
            local args = {[1] = "getquests", [2] = {}}
            local quests = ReplicatedStorage.Remotes.CommF_:InvokeServer(unpack(args))
            if quests then
                for _, quest in pairs(quests) do
                    if quest.LevelReq <= LocalPlayer.Data.Level.Value then
                        ReplicatedStorage.Remotes.CommF_:FireServer("startquest", quest.Id)
                        wait(1)
                        break
                    end
                end
            end
        end)
    end

    -- FAST ATTACK
    local function fastAttack()
        if not fastAttackEnabled then return end
        pcall(function()
            ReplicatedStorage.Remotes.CommF_:FireServer("KenHaki")
            ReplicatedStorage.Remotes.CommF_:FireServer("FlashStep")
        end)
    end

    -- NO CLIP
    local function noClip()
        if not noClipEnabled then return end
        if LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart") then
            LocalPlayer.Character.HumanoidRootPart.CanCollide = false
        end
    end

    -- ESP (Wallhack)
    local espLabels = {}
    local function updateESP()
        for _, player in pairs(Players:GetPlayers()) do
            if player ~= LocalPlayer then
                if not espLabels[player] then
                    local label = Instance.new("TextLabel")
                    label.Size = UDim2.new(0, 120, 0, 22)
                    label.BackgroundColor3 = Color3.new(0, 0, 0)
                    label.BackgroundTransparency = 0.5
                    label.BorderSizePixel = 1
                    label.Parent = screenGui
                    label.Font = Enum.Font.GothamBold
                    label.TextSize = 13
                    espLabels[player] = label
                end
                local label = espLabels[player]
                if espEnabled and player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
                    local rootPos, onScreen = Camera:WorldToViewportPoint(player.Character.HumanoidRootPart.Position)
                    if onScreen then
                        label.TextColor3 = Color3.new(1, 0, 0)
                        label.Text = player.Name
                        label.Position = UDim2.new(0, rootPos.X - 60, 0, rootPos.Y - 40)
                        label.Visible = true
                    else
                        label.Visible = false
                    end
                else
                    if label then label.Visible = false end
                end
            end
        end
    end

    -- RESET COLLISION
    spawn(function()
        while true do
            if not noClipEnabled and LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart") then
                LocalPlayer.Character.HumanoidRootPart.CanCollide = true
            end
            wait(1)
        end
    end)

    -- MAIN LOOP
    RunService.RenderStepped:Connect(function()
        updateESP()
        if autoFarmEnabled then autoFarm() end
        if oneHitKillEnabled then oneHitKill() end
        if autoCollectEnabled then autoCollect() end
        if teleportToBossEnabled then teleportToBoss() end
        if autoQuestEnabled then autoQuest() end
        if fastAttackEnabled then fastAttack() end
        if noClipEnabled then noClip() end
    end)

    -- BUTTON HANDLERS
    espBtn.Activated:Connect(function()
        espEnabled = not espEnabled
        espBtn.Text = espEnabled and "ESP: ON" or "ESP: OFF"
        espBtn.BackgroundColor3 = espEnabled and Color3.new(0.2, 0.8, 0.2) or Color3.new(0.6, 0.2, 0.2)
        if not espEnabled then
            for _, label in pairs(espLabels) do
                if label then label.Visible = false end
            end
        end
    end)

    farmBtn.Activated:Connect(function()
        autoFarmEnabled = not autoFarmEnabled
        farmBtn.Text = autoFarmEnabled and "AUTO FARM: ON" or "AUTO FARM: OFF"
        farmBtn.BackgroundColor3 = autoFarmEnabled and Color3.new(0.2, 0.8, 0.2) or Color3.new(0.6, 0.2, 0.2)
    end)

    oneHitBtn.Activated:Connect(function()
        oneHitKillEnabled = not oneHitKillEnabled
        oneHitBtn.Text = oneHitKillEnabled and "1 HIT KILL: ON" or "1 HIT KILL: OFF"
        oneHitBtn.BackgroundColor3 = oneHitKillEnabled and Color3.new(0.2, 0.8, 0.2) or Color3.new(0.6, 0.2, 0.2)
    end)

    collectBtn.Activated:Connect(function()
        autoCollectEnabled = not autoCollectEnabled
        collectBtn.Text = autoCollectEnabled and "AUTO COLLECT: ON" or "AUTO COLLECT: OFF"
        collectBtn.BackgroundColor3 = autoCollectEnabled and Color3.new(0.2, 0.8, 0.2) or Color3.new(0.6, 0.2, 0.2)
    end)

    bossBtn.Activated:Connect(function()
        teleportToBossEnabled = not teleportToBossEnabled
        bossBtn.Text = teleportToBossEnabled and "TELEPORT BOSS: ON" or "TELEPORT BOSS: OFF"
        bossBtn.BackgroundColor3 = teleportToBossEnabled and Color3.new(0.2, 0.8, 0.2) or Color3.new(0.6, 0.2, 0.2)
    end)

    questBtn.Activated:Connect(function()
        autoQuestEnabled = not autoQuestEnabled
        questBtn.Text = autoQuestEnabled and "AUTO QUEST: ON" or "AUTO QUEST: OFF"
        questBtn.BackgroundColor3 = autoQuestEnabled and Color3.new(0.2, 0.8, 0.2) or Color3.new(0.6, 0.2, 0.2)
    end)

    fastBtn.Activated:Connect(function()
        fastAttackEnabled = not fastAttackEnabled
        fastBtn.Text = fastAttackEnabled and "FAST ATTACK: ON" or "FAST ATTACK: OFF"
        fastBtn.BackgroundColor3 = fastAttackEnabled and Color3.new(0.2, 0.8, 0.2) or Color3.new(0.6, 0.2, 0.2)
    end)

    noclipBtn.Activated:Connect(function()
        noClipEnabled = not noClipEnabled
        noclipBtn.Text = noClipEnabled and "NO CLIP: ON" or "NO CLIP: OFF"
        noclipBtn.BackgroundColor3 = noClipEnabled and Color3.new(0.2, 0.8, 0.2) or Color3.new(0.6, 0.2, 0.2)
    end)

    -- HIDE/SHOW
    hideBtn.Activated:Connect(function()
        mainFrame.Visible = false
        floatingCircle.Visible = true
    end)

    floatingCircle.Activated:Connect(function()
        mainFrame.Visible = true
        floatingCircle.Visible = false
    end)

    -- DRAG
    local dragStart, frameStart, isDragging
    mainFrame.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.Touch then
            isDragging = true
            dragStart = input.Position
            frameStart = mainFrame.Position
        end
    end)
    UserInputService.InputChanged:Connect(function(input)
        if isDragging and input.UserInputType == Enum.UserInputType.Touch then
            local delta = input.Position - dragStart
            mainFrame.Position = UDim2.new(frameStart.X.Scale, frameStart.X.Offset + delta.X, frameStart.Y.Scale, frameStart.Y.Offset + delta.Y)
        end
    end)
    UserInputService.InputEnded:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.Touch then
            isDragging = false
        end
    end)

    local circleDragStart, circleStartPos, isCircleDragging
    floatingCircle.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.Touch then
            isCircleDragging = true
            circleDragStart = input.Position
            circleStartPos = floatingCircle.Position
        end
    end)
    UserInputService.InputChanged:Connect(function(input)
        if isCircleDragging and input.UserInputType == Enum.UserInputType.Touch then
            local delta = input.Position - circleDragStart
            floatingCircle.Position = UDim2.new(circleStartPos.X.Scale, circleStartPos.X.Offset + delta.X, circleStartPos.Y.Scale, circleStartPos.Y.Offset + delta.Y)
        end
    end)
    UserInputService.InputEnded:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.Touch then
            isCircleDragging = false
        end
    end)

    -- CLEANUP
    Players.PlayerRemoving:Connect(function(player)
        if espLabels[player] then
            espLabels[player]:Destroy()
            espLabels[player] = nil
        end
    end)

    print("BLOX FRUITS PAID v1.0 LOADED - ALL FEATURES WORKING")
end

-- PASSWORD CHECK
local function checkPassword()
    if passwordBox.Text == "astro" then
        loadMainScript()
    else
        errorText.Text = "Incorrect password"
    end
end

submitBtn.MouseButton1Click:Connect(checkPassword)
passwordBox.FocusLost:Connect(checkPassword)

-- ========== FEATURES ==========
-- 1. ESP (Wallhack): See all players through walls (Red = Enemy)
-- 2. AUTO FARM: Automatically finds and teleports to nearest enemy, then attacks
-- 3. 1 HIT KILL: Instantly kills any enemy or boss with one hit
-- 4. AUTO COLLECT: Automatically picks up fruits, chests, drops, and beli within 80 studs
-- 5. TELEPORT TO BOSS: Instantly teleports to nearest boss (Dragon, Don Swan, Magma, etc.)
-- 6. AUTO QUEST: Automatically accepts available quests based on your level
-- 7. FAST ATTACK: Removes attack cooldown (KenHaki + FlashStep)
-- 8. NO CLIP: Walk through walls, obstacles, and terrain
-- 9. FPS BOOSTER: Lowers graphics, removes shadows/decals/particles for better performance
-- 10. HIDE/SHOW GUI: Press "━" button to hide menu into floating circle "B", tap circle to restore
-- 11. DRAGGABLE GUI: Move the menu anywhere on screen by dragging
-- 12. PASSWORD PROTECTION: Password is "astro"
-- 13. All features toggle ON/OFF via buttons (ESP default ON, others OFF)
